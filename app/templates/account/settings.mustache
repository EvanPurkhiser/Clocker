<section class="container">
  <div class="page-header">
    <h3>Account Settings <small>Manage email and password</small></h3>
  </div>

  <div class="row-fluid">
    <div class="span4">
      <form method="post" action="/account/settings/change_email" class="form-vertical form-popdown-errors">
        <div class="control-group {{#errors.email}}error{{/errors.email}}">
          <label for="change_email_email" class="control-label">New Email</label>
          <div class="controls">
            <input type="email" id="change_email_email" name="email" />
            <span class="help-inline">{{errors.email}}</span>
          </div>
        </div>
        <div class="control-group">
          <label for="change_email_password" class="control-label">Current Password</label>
          <div class="controls">
            <input type="password" id="change_email_password" name="password_confirm" />
          </div>
        </div>

        <button type="submit" class="btn btn-primary"><i class="icon-envelope-alt"></i> Change Email</button>
      </form>
    </div>

    <div class="span4">
      <form method="post" action="/account/settings/change_password" class="form-vertical form-popdown-errors">
        <div class="control-group {{#errors.password}}error{{/errors.password}}">
          <label for="change_password_new_password" class="control-label">New Password</label>
          <div class="controls">
            <input type="password" id="change_password_new_password" name="password" />
            <span class="help-inline">{{errors.password}}</span>
          </div>
        </div>
        <div class="control-group">
          <label for="change_password_password" class="control-label">Current Password</label>
          <div class="controls">
            <input type="password" id="change_password_password" name="password_confirm" />
          </div>
        </div>

        <button type="submit" class="btn btn-primary"><i class="icon-key"></i> Change Password</button>
      </form>
    </div>

    <div class="span4">
      <h4>I want to delete my account</h4>
      <p>
        Not a problem! We dig keeping your virtual life clean and organized. But were not going to
        keep all of your stuff around. When you delete your account, all of your projects and tracked hours will go
        poof.
      </p>
      <a href="#delete-account-prompt" data-toggle="modal" class="btn btn-danger">
        <i class="icon-remove-sign"></i> Yes, delete my account
      </a>

      <div id="delete-account-prompt" class="modal hide fade">
        <div class="modal-header">
          <a href="#" class="close" data-dismiss="modal">&times;</a>
          <h3>Delete my account</h3>
        </div>
        <form method="post" action="/account/settings/delete">
          <div class="modal-body">
            <p>
              Well if you're sure... Just type in your password and we'll do it.
            </p>
            <input type="password" name="password_confirm" class="input-block-level" />
          </div>
          <div class="modal-footer">
            <button type="submit" class="btn btn-danger">I'm absolutely sure. Delete it!</button>
            <button class="btn" data-dismiss="modal">No Don't!</button>
          </div>
        </form>
      </div>
    </div>
  </div>
</section>
